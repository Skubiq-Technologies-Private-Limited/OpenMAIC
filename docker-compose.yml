services:
  openmaic:
    image: ${OPENMAIC_IMAGE:-ghcr.io/thu-maic/openmaic:main}
    pull_policy: always
    container_name: openmaic_prod
    env_file:
      - .env.local
    volumes:
      - ./data:/app/data
    expose:
      - "3000"
    networks:
      - edukube_default
    restart: unless-stopped

networks:
  edukube_default:
    external: true