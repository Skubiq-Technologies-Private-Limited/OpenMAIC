services:
  openmaic:
    build: .
    container_name: openmaic_prod
    env_file:
      - .env.local
    volumes:
      - ./data:/app/data
    expose:
      - "3000"
    networks:
      - edukube_default
    restart: unless-stopped

networks:
  edukube_default:
    external: true